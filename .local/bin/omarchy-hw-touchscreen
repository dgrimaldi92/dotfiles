#!/bin/bash

# omarchy:summary=Print the detected Hyprland touchscreen or tablet device name

device=$(hyprctl devices -j | jaq -r '[.touch[]?.name, .tablets[]?.name] | first // empty')
[[ -n $device ]] && echo "$device"
